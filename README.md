# Sum of Numbers From 1-N
## Description
This project is a Python program that demonstrates the use of while loop in Python. A while loop repeatedly executes a block of code as long as **a specified condition is True**. It is useful when the number of iterations is not fixed or depends on a condition.
## Syntax
```python
while condition:
   # Code to execute if condition is True
```
## Concepts Used
- Variables
- while Loop
- Comparison Operators
- Increment and Decrement
## Technologies Used
- Pycharm
- Python 3
## Working of the Code
1. The variable 'n' is initialized with the value '5'.
2. The variable 'i' is initialized with the value '1'.
3. The variable 'sum' is initialized with the value '0'.
4. The 'while' loop runs as long as 'i <= n'.
5. The value of 'sum' is incremented by 'i'.
6. The value of 'i' is incremented by '1'.
7. The loop continues until the condition 'i <= n' becomes false.
9. Finally, the sum of numbers from 1 to N is printed.
## How to run
1. Clone the repository.
2. Open the project in pycharm.
3. Run the 'main.py' file.
4. View the result.
## Sample Output
```text

Sum is: 15
```
## Author
- Sadia
- Github: [sadiacode](https://github.com/sadiacode)
